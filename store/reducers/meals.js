import {MEALS} from "../../data/data";

const initialState = {
    meals: MEALS,
    filteredMeals: MEALS,
    favoriteMeals: [],
}

const mealsReducer = (state, action) => {
    return state
}

export default mealsReducer
